package com.stalemated.lib.util.state;

public class SharedTooltipState {
    /*If installed, STS will force a tooltip to abide by
    the custom dimensions rules when another mod modifies this value*/
    public static boolean forceCustomDimensions = false;
}